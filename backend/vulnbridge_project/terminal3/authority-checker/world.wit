package vulnbridge:authority@1.0.0;

interface kv-store {
    get: func(map-tail: string, key: string) -> option<string>;
}

world authority-checker {
    import kv-store;
    export check-authority: func(action: string, map-tail: string) -> bool;
    export get-authority-record: func(action: string, map-tail: string) -> option<string>;
}
