/* 
  Empty postcss.config.js — CRA react-scripts 5 manages PostCSS internally
  for tailwindcss v3. Do not add plugins here as CRA ignores user postcss
  configs and uses its own bundled chain.
*/
module.exports = {};
